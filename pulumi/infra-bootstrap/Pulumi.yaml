name: infra-bootstrap
description: The PKO managed with helm to get started
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-python
