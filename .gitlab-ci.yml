# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence
stages:
  - test # template security scans (semgrep-sast, secret_detection) default here
  - build # build + push our custom container images
  - scan # container scanning of the images we just built
  - maintenance

# Only include the security scan templates in non-scheduled pipelines. This is
# decided at config time by $CI_PIPELINE_SOURCE, which is set by GitLab itself
# (only the scheduler produces "schedule" pipelines). We deliberately do NOT
# use SAST_DISABLED / SECRET_DETECTION_DISABLED variables: pipeline variables
# (Run pipeline form, `git push -o ci.variable=...`) take highest precedence
# and would let anyone skip the scans on demand.
include:
  - template: Security/SAST.gitlab-ci.yml
    rules:
      - if: $CI_PIPELINE_SOURCE != "schedule"
  - template: Security/Secret-Detection.gitlab-ci.yml
    rules:
      - if: $CI_PIPELINE_SOURCE != "schedule"
  - template: Security/Container-Scanning.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: "true"
  # THE ODD ONE OUT: all versions belong in pulumi/versions.yaml, EXCEPT this one.
  # The .NET runtime tag is the sole BUILD-time version (the vints-server base
  # image); it's pinned to the exact patch here so renovate bumps it monthly and CI
  # rebuilds. Please keep every other versioned thing in versions.yaml, not here.
  # renovate: datasource=docker depName=mcr.microsoft.com/dotnet/runtime
  DOTNET_TAG: "10.0.11"

# Automatically update dependencies via Renovate
renovate:
  stage: maintenance
  image:
    # Renovate's gitlabci manager will update this
    name: renovate/renovate:44.14.7
    entrypoint: [""] # the image entrypoint is the renovate binary itself
  variables:
    RENOVATE_PLATFORM: "gitlab"
    RENOVATE_REPOSITORIES: $CI_PROJECT_PATH
  script:
    - renovate
  only:
    - schedules

# Build + push our custom images to GitLab Container Registry.
#
# vints-server is built from the pinned DOTNET_TAG (the build-time base image).
# It rebuilds on server source changes OR on a CI change (renovate bumps the
# DOTNET_TAG var, which lives here as the odd-one-out version).
build:vints:
  stage: build
  image: quay.io/containers/buildah:latest
  script:
    - >
      buildah build --storage-driver=vfs --isolation=chroot
      --build-arg DOTNET_TAG="$DOTNET_TAG"
      --tag "$CI_REGISTRY_IMAGE/vints-server:latest" pulumi/my-apps/containers/server
    - >
      buildah push --storage-driver=vfs
      --creds "$CI_REGISTRY_USER:$CI_REGISTRY_PASSWORD"
      "$CI_REGISTRY_IMAGE/vints-server:latest"
      "docker://$CI_REGISTRY_IMAGE/vints-server:latest"
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - pulumi/my-apps/containers/server/**
        # Rebuild when the build-time .NET tag changes (renovate bumps it above).
        - .gitlab-ci.yml
    - when: manual

# vints-backup is fully independent: no build-time version inputs, so it is built
# only when its own sources change (never on version / .NET bumps).
build:vints-backup:
  stage: build
  image: quay.io/containers/buildah:latest
  script:
    - >
      buildah build --storage-driver=vfs --isolation=chroot
      --tag "$CI_REGISTRY_IMAGE/vints-backup:latest" pulumi/my-apps/containers/backup
    - >
      buildah push --storage-driver=vfs
      --creds "$CI_REGISTRY_USER:$CI_REGISTRY_PASSWORD"
      "$CI_REGISTRY_IMAGE/vints-backup:latest"
      "docker://$CI_REGISTRY_IMAGE/vints-backup:latest"
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - pulumi/my-apps/containers/backup/**
    - when: manual

# Container scanning: one separate (non-matrix) scan job per image, each pointing
# at its own image + Dockerfile. Discrete jobs so the server rescans on a .NET bump
# while the backup scans only when its own sources change.
container_scanning:
  rules:
    - when: never

container_scanning:vints:
  extends: container_scanning
  stage: scan
  variables:
    CS_IMAGE: "$CI_REGISTRY_IMAGE/vints-server:latest"
    CS_DOCKERFILE_PATH: "pulumi/my-apps/containers/server/Dockerfile"
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - pulumi/my-apps/containers/server/**
        # Rescan when the build-time .NET tag bumps (renovate edits .gitlab-ci.yml).
        - .gitlab-ci.yml
    - when: manual

container_scanning:vints-backup:
  extends: container_scanning
  stage: scan
  variables:
    CS_IMAGE: "$CI_REGISTRY_IMAGE/vints-backup:latest"
    CS_DOCKERFILE_PATH: "pulumi/my-apps/containers/backup/Dockerfile"
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - pulumi/my-apps/containers/backup/**
    - when: manual
