# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence
stages:
  - test
  - secret-detection
  - pulumi
# Shared rules for the template security scans: every pipeline except scheduled ones.
.no-schedule-rules: &no-schedule-rules
  - if: $CI_PIPELINE_SOURCE == "schedule"
    when: never
  - when: on_success

sast:
  stage: test
  rules: *no-schedule-rules
include:
  - template: Security/SAST.gitlab-ci.yml
  - template: Security/Secret-Detection.gitlab-ci.yml
variables:
  SECRET_DETECTION_ENABLED: "true"
secret_detection:
  stage: secret-detection
  rules: *no-schedule-rules

preview:
  stage: pulumi
  image: pulumi/pulumi-python:latest
  script:
    - curl -LsSf https://astral.sh/uv/install.sh | sh
    - uv sync
    - uv run pulumi preview -s bmbeverst/infra-apps/dev --cwd pulumi/infra-apps
  only:
    - merge_request

# Automatically update dependencies via Renovate
renovate:
  stage: test
  image:
    name: renovate/renovate:latest
    entrypoint: [""] # the image entrypoint is the renovate binary itself
  variables:
    RENOVATE_PLATFORM: "gitlab"
    RENOVATE_REPOSITORY: $CI_PROJECT_PATH
  script:
    - renovate
  only:
    - schedules
